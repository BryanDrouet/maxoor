# maxoor

La mesure d'audience gratuite est branchée côté site et s'active après consentement. Renseigner un Measurement ID Google Analytics 4 via `window.MAXOOR_ANALYTICS_ID` ou la meta `maxoor-analytics-id` pour l'activer.